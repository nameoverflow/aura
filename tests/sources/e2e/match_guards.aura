def classify(x: Int) -> Int = match x {
    n if n < 0 => 0,
    n if n == 0 => 1,
    n if n < 10 => 2,
    _ => 3
}

def main() -> Int = {
    println(classify(-5));
    println(classify(0));
    println(classify(7));
    println(classify(100));
    0
}
